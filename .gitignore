.DS_Store
**/.DS_Store

# Python virtualenvs
**/.tts-venv/
**/.venv/
**/venv/

# Supabase — local dev credentials (founder passwords). Do not commit.
supabase/bootstrap-users.sql

# HyperFrames auto-generated preview thumbnails — regenerated on every render.
**/.thumbnails/

# Microsoft Office lock files (open doc → ~$filename.docx/pptx).
~$*

# Bundles staged for export to other repos (move them out, don't commit here).
_export/
